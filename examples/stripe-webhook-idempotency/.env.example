# Stripe webhook endpoint secret (from Stripe Dashboard -> Developers -> Webhooks).
# In tests this is NOT used: the route accepts a `test_secret` / `test_sig` shortcut
# gated on NODE_ENV=test. See src/lib/stripe-verify.ts.
STRIPE_WEBHOOK_SECRET=whsec_

# In production, set NODE_ENV=production so the test signature shortcut is disabled.
# NODE_ENV=production